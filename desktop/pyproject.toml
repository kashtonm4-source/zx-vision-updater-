[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "true-vision-desktop"
version = "1.0.0.dev0"
description = "TRUE VISION native desktop companion"
requires-python = ">=3.11"
dependencies = ["PySide6>=6.7,<7"]

[tool.pytest.ini_options]
pythonpath = ["."]
